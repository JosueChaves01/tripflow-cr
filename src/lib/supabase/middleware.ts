/**
 * Session refresh logic lives in src/middleware.ts (Next.js middleware file).
 * This shim exists so agents can import from @/lib/supabase/middleware without errors.
 * The actual middleware is at the src/ root as required by Next.js.
 */
export { }
